// Write a program to calculate the area of the square where side is given

#include <stdio.h>

int main()
{
    int Side;
    printf("Enter the value of the Side of the square: ");
    scanf("%d", &Side);
    printf("The Area of the square is %d \n", Side * Side);
}